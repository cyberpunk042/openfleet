--- a/backend/app/schemas/agents.py
+++ b/backend/app/schemas/agents.py
@@ -268,6 +268,10 @@
         description="Agent health status string.",
         examples=["healthy", "offline", "degraded"],
     )
+    message: str | None = Field(
+        default=None,
+        description="Optional suffix appended to the activity message (e.g. '(silent)').",
+    )


 class AgentHeartbeatCreate(AgentHeartbeat):
--- a/backend/app/services/openclaw/provisioning_db.py
+++ b/backend/app/services/openclaw/provisioning_db.py
@@ -940,11 +940,14 @@
         OpenClawAuthorizationPolicy.require_board_write_access(allowed=allowed)

     @staticmethod
-    def record_heartbeat(session: AsyncSession, agent: Agent) -> None:
+    def record_heartbeat(session: AsyncSession, agent: Agent, message_suffix: str | None = None) -> None:
+        msg = f"Heartbeat received from {agent.name}."
+        if message_suffix:
+            msg = f"Heartbeat received from {agent.name}. {message_suffix}"
         record_activity(
             session,
             event_type="agent.heartbeat",
-            message=f"Heartbeat received from {agent.name}.",
+            message=msg,
             agent_id=agent.id,
             board_id=agent.board_id,
         )
@@ -1433,6 +1436,7 @@
         *,
         agent: Agent,
         status_value: str | None,
+        message_suffix: str | None = None,
     ) -> AgentRead:
         if status_value:
             agent.status = status_value
@@ -1444,7 +1448,7 @@
         agent.checkin_deadline_at = None
         agent.last_provision_error = None
         agent.updated_at = utcnow()
-        self.record_heartbeat(self.session, agent)
+        self.record_heartbeat(self.session, agent, message_suffix=message_suffix)
         self.session.add(agent)
         await self.session.commit()
         await self.session.refresh(agent)
@@ -1679,6 +1683,7 @@
         return await self.commit_heartbeat(
             agent=agent,
             status_value=payload.status,
+            message_suffix=payload.message,
         )

     async def heartbeat_or_create_agent(
